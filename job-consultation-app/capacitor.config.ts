import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jobconsultation.app',
  appName: 'Job Consultation',
  webDir: 'dist/job-consultation-app'
};

export default config;
